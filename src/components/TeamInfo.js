import React from 'react'
import styled from 'styled-components'

const TeamInfo = () => {
  return <TeamInfoBox>TeamInfo</TeamInfoBox>
}

export default TeamInfo

const TeamInfoBox = styled.div`
  box-sizing: border-box;
  height: 100%;
  width: 600px;
  padding: 30px;
  background: white;
  `